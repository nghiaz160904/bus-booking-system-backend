# Bus Booking System Backend

### 1. Create a GitHub Fine-grained token for this project
Then add the token to .env file
``` .env
GITHUB_PAT=TOKEN
```

### 2. Build the projects

```
./gradlew build -x test
```

### 3. Run with Docker
```
docker-compose up -d
```